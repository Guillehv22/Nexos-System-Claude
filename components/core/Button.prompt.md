Pill-shaped call-to-action button in 4 variants and 3 sizes. Intentional addition — not in source tokens.json, added because decks/UI need CTAs.

```jsx
<Button variant="primary" size="md">Agendar reunión</Button>
```

Variants: primary (solid blue), secondary (light gray), ghost (blue outline), dark (on dark surfaces).
