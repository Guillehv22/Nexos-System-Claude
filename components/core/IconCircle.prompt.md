Circle-outline icon frame (never a solid background) for checkmarks or process numbers. Matches tokens.json `components.iconValidated`.

```jsx
<IconCircle>✓</IconCircle>
```

Stroke is always brand blue by default; swap `color` for on-dark contexts only if needed.
