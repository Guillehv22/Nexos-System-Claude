Numbered blue circles connected by arrows, for flow/process slides. Matches tokens.json `components.processArrow`.

```jsx
<ProcessSteps steps={['Diagnóstico', 'Estrategia', 'Ejecución']} />
```

Pass step labels only for count/order; render labels below externally if needed.
